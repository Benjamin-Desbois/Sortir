<!doctype html>
<html lang="en">
<head>
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">
    <link rel="icon" href="{{ asset('/images/porte_ouverte.png') }}" type="image/gif" sizes="16x16">
    <meta charset="UTF-8">
    <title>Login</title>
</head>
<body>
<div class="container">
    <header>
        <nav class="navbar navbar-dark bg-dark">
            <div class="container-fluid justify-content-start">
                <img class="logo" src="{{ asset('/images/porte_ouverte.png') }}" width="50px">
                <a class="navbar-brand">Sortir.com</a>
            </div>
        </nav>
    </header>
    <div class="justify-content-center">
        <form method="post">
            {% if error %}
                <div class="alert alert-danger">{{ 'Le nom d\'utilisateur ou le mot de passe est incorrect' }}</div>
            {% endif %}

            {% if app.user %}
                <div class="mb-3">
                    You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                </div>
            {% endif %}

            <h1 class="h3 mb-3 font-weight-normal text-center">Bienvenue</h1>
            <div class="row">
                <div class="col-1 offset-3 justify-content-end">
                    <label for="inputPseudo">Pseudo</label>
                </div>
                <div class="col-4">
                    <input type="text" value="{{ last_username }}" name="pseudo" id="inputPseudo" class="form-control"
                           autocomplete="username" required autofocus>
                </div>
            </div>
            <div class="row">
                <div class="col-1 offset-3 justify-content-end">
                    <label for="inputPassword">Password</label>
                </div>
                <div class="col-4">
                    <input type="password" name="password" id="inputPassword" class="form-control"
                           autocomplete="current-password"
                           required>
                </div>
            </div>

            <div class="row">
                <div class="col offset-3">
                    <div class="checkbox mb-3">
                        <label>
                            <input type="checkbox" name="_remember_me"> Se souvenir de moi
                        </label>
                    </div>
                </div>
            </div>

            <div class="d-grid gap-2 col-2 mx-auto">
                <button class="btn btn-lg btn-dark" type="submit">Connexion</button>
                <a href="{{ path('app_register') }}" class="btn btn-lg btn-dark" role="button" data-bs-toggle="button">Inscription</a>
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            {#
            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html
            #}

        </form>
    </div>
    <footer class="py-3 my-4">
        <hr>
        <p class="text-center text-muted">Pour un événement à votre image</p>
        <p class="text-center text-muted">&copy; {{ "now"|date("Y") }} Sorteam, Inc</p>
    </footer>
</div>
</body>
</html>